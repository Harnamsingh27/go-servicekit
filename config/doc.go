// Package config provides a generic, multi-source configuration loader.
// It supports loading from YAML files, .env files, environment variables,
// and struct-tag defaults, with a clear precedence order.
package config
